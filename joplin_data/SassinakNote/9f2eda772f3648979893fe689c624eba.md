id: 9f2eda772f3648979893fe689c624eba
parent_id: 
item_type: 1
item_id: ef7bee2c77214aab9f5411553eef612d
item_updated_time: 1754576827402
title_diff: "[{\"diffs\":[[1,\"编译器下的lambda表达式\"]],\"start1\":0,\"start2\":0,\"length1\":0,\"length2\":14}]"
body_diff: "[{\"diffs\":[[1,\"# 编译器下的lambda表达式\\\n\\\n在编译器的眼里，lambda表达式会被翻译成一个**类**，使用重载来实现。\\\n\\\n例如：\\\n~~~\\\nauto plus = [] (int a, int b) -> int { return a + b; }\\\nint c = plus(1, 2);\\\n~~~\\\n\\\n会被翻译成\\\n~~~\\\nclass LambdaClass\\\n{\\\npublic:\\\n    int operator () (int a, int b) const\\\n    {\\\n        return a + b;\\\n    }\\\n};\\\n\\\nLambdaClass plus;\\\nint c = plus(1, 2);\\\n~~~\\\n\\\n>调用的时候编译器会生成一个Lambda的对象，并调用opeartor ()函数。（备注：这里的编译的翻译结果并不和真正的结果完全一致，只是把最主要的部分体现出来，其他的像类到函数指针的转换函数均省略）\\\n\\\n- 在有捕获时，外部的变量会在**类**里面生成一个相同的变量\\\n- 当没有捕获时，lambda将直接转换为函数指针。\\\n- 在**值捕获**时，是无法修改捕获的值的，如果想要在函数内修改捕获的值，需要加上关键字`mutable`\\\n~~~\\\n int x = 1; int y = 2;\\\nauto plus = [=] (int a, int b) mutable -> int { x++; return x + y + a + b; };\\\nint c = plus(1, 2);\\\n~~~\\\n\\\n- 在使用引用捕获时，使用**参数引用**的方式进行传递\\\n- 会**直接修改**外部变量的值\\\n\\\n***\\\n针对上面的集中情况，我们把lambda的各个成分和类的各个成分对应起来就是如下的关系:\\\n\\\n1. 捕获列表，对应LambdaClass类的`private`成员。\\\n\\\n2. 参数列表，对应LambdaClass类的成员函数的`operator()`的形参列表\\\n\\\n3. `mutable`，对应 LambdaClass类成员函数 `operator()` 的`const`属性 ，但是只有在捕获列表捕获的参数**不含有引用捕获**的情况下才会生效，因为捕获列表只要包含引用捕获，那`operator()`函数就一定是非`const`函数。\\\n\\\n4. 返回类型，对应 LambdaClass类成员函数 `operator()`的返回类型\\\n\\\n5. 函数体，对应 LambdaClass类成员函数 `operator()` 的函数体。\\\n\\\n6. 引用捕获和值捕获不同的一点就是，对应的成员是否为引用类型。\\\n \"]],\"start1\":0,\"start2\":0,\"length1\":0,\"length2\":1101}]"
metadata_diff: {"new":{"id":"ef7bee2c77214aab9f5411553eef612d","parent_id":"e8556170c65042f68af3dc9d91be8ebf","latitude":"0.00000000","longitude":"0.00000000","altitude":"0.0000","author":"","source_url":"","is_todo":0,"todo_due":0,"todo_completed":0,"source":"joplin-desktop","source_application":"net.cozic.joplin-desktop","application_data":"","order":1754577291566,"markup_language":1,"is_shared":0,"share_id":"","conflict_original_id":"","master_key_id":"","user_data":"","deleted_time":0},"deleted":[]}
encryption_cipher_text: 
encryption_applied: 0
updated_time: 2025-08-07T14:39:40.974Z
created_time: 2025-08-07T14:39:40.974Z
type_: 13