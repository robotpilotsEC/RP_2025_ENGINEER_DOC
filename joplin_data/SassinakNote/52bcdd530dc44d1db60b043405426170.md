id: 52bcdd530dc44d1db60b043405426170
parent_id: 
item_type: 1
item_id: f34efb4650684d90bd9880fa75c81358
item_updated_time: 1754576827401
title_diff: "[{\"diffs\":[[1,\"decltype\"]],\"start1\":0,\"start2\":0,\"length1\":0,\"length2\":8}]"
body_diff: "[{\"diffs\":[[1,\"# decltype\\\n\\\n## 前言：\\\n `auto` 是设计出来**获取变量类型**的，若仅仅希望得到类型而**不需要定义变量**就需要`decltype`关键字\\\n\\\n`decltype`是在编译期完成推导，不会真正计算表达式的值。\\\n\\\n```\\\nint x = 0;\\\ndecltype(x) y = 1; // y -> int\\\ndecltype(x + y) z = 0; // z -> int\\\nconst int& i = x;\\\ndecltype(i) j = y; // j -> const int &\\\nconst decltype(z) * p = &z; // *p -> const int, p -> const int *\\\ndecltype(z) * pi = &z; // *pi -> int , pi -> int *\\\ndecltype(pi)* pp = &pi; // *pp -> int * , pp -> int * *\\\n```\\\n\\\n上述的例子说明：\\\n1. `decltype`相比于`auto`可以获取表达式的类型而不与表达式具有相同的值\\\n2.  可以保留表达式的引用和`const`\\\n3.  可以获得二维指针\\\n\\\n\\\n***\\\n```\\\nstruct Foo { int x; };\\\nconst Foo foo = Foo();\\\ndecltype(foo.x) a = 0; // a -> int\\\ndecltype((foo.x)) b = a; // b -> const int &\\\nint n = 0, m = 0;\\\ndecltype(n + m) c = 0; // c -> int\\\ndecltype(n += m) d = c; // d -> int &\\\n```\\\n\\\n- a 和b 的结果：仅仅多加了一对括号，它们得到的类型却是不同的。\\\n- a 的结果是很直接的，根据推导规则1，a 的类型就是foo.x 的定义类型。\\\n- b 的结果并不适用于推导规则1 和2。根据`foo.x` 是一个左值，可知括号表达式也是一个 左值。因此可以按照推导规则3，知道`decltype` 的结果将是一个左值引用。 `foo` 的定义是`const Foo`，所以`foo.x` 是一个`const int` 类型左值，因此`decltype` 的推导结果 是`const int &`。 同样，n+m 返回一个右值，按照推导规则3，`decltype` 的结果为int。 最后，n+=m 返回一个左值，按照推导规则3，`decltype` 的结果为`int &`。\\\n\\\n***  \\\n在C++11 中增加了返回类型后置`（trailing-return-type，又称跟踪返回类型）`语法，将`decltype` 和`auto` 结合起来完成返回值类型的推导。\\\n```\\\ntemplate <typename T, typename U>\\\nauto add(T t, U u) -> decltype(t + u)\\\n{\\\nreturn t + u;\\\n}\\\n```\\\n\\\n\"]],\"start1\":0,\"start2\":0,\"length1\":0,\"length2\":1291}]"
metadata_diff: {"new":{"id":"f34efb4650684d90bd9880fa75c81358","parent_id":"8bd4b59dbf744ed8af44fa7a66224cb2","latitude":"0.00000000","longitude":"0.00000000","altitude":"0.0000","author":"","source_url":"","is_todo":0,"todo_due":0,"todo_completed":0,"source":"joplin-desktop","source_application":"net.cozic.joplin-desktop","application_data":"","order":1754577291488,"markup_language":1,"is_shared":0,"share_id":"","conflict_original_id":"","master_key_id":"","user_data":"","deleted_time":0},"deleted":[]}
encryption_cipher_text: 
encryption_applied: 0
updated_time: 2025-08-07T14:39:40.933Z
created_time: 2025-08-07T14:39:40.933Z
type_: 13