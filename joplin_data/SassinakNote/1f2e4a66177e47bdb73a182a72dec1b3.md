id: 1f2e4a66177e47bdb73a182a72dec1b3
parent_id: 
item_type: 1
item_id: edb9b1f671954234bb9086c0492485e6
item_updated_time: 1754576866203
title_diff: "[{\"diffs\":[[1,\"设备树\"]],\"start1\":0,\"start2\":0,\"length1\":0,\"length2\":3}]"
body_diff: "[{\"diffs\":[[1,\"# 设备树\\\n\\\n## 简介\\\n- 设备树（Device Tree）用于描述计算机（微机）的特定硬件设备信息的数据结构。通常以.dts格式存在\\\n- 设备树在不改变Linux内核下，对不同的计算平台实现支持\\\n***\\\n## 基本结构\\\n1. 根节点\\\n~~~\\\n{\\\n\\t/*属性，子节点*/\\\n}\\\n~~~\\\n2. 子节点，待变各种硬件组件，包括CPU，内存，GPIO\\\n~~~\\\nCPU@{\\\n\\t/*属性*/\\\n}\\\n~~~\\\n3. 节点属性，以**键值对**的形式存在，描述节点的特定信息，如寄存器地址，时钟\\\n4. `@`后如果有地址必须和第一个寄存器地址相同，如果没有，必须**省略**\\\n\\\n ***\\\n## 实例分析\\\n~~~\\\n/dts-v1/; //这是设备树的版本，必须写在第一行\\\n\\\n/include/ \\\"mt7628an.dtsi\\\" //引用的头文件，用于描述一个soc的通用信息（比如cpu、soc、中断控制等等）\\\n\\\n/ {\\\n    // compatible属性定义了设备的兼容性，通常包含特定于平台或板卡的字符串\\\n    compatible = \\\"mediatek,mt7628an-eval-board\\\", \\\"mediatek,mt7628an-soc\\\";\\\n    // model属性定义了设备的型号，用于标识硬件平台\\\n    model = \\\"Mediatek MT7628AN evaluation board\\\";\\\n\\\n    // chosen节点用于定义一些系统级的配置选项\\\n    chosen {\\\n            // bootargs属性定义了内核启动时的参数，例如串口控制台的配置\\\n            bootargs = \\\"console=ttyS0,115200\\\";\\\n    };\\\n\\\n    // memory节点定义了内存的配置\\\n    memory@0 {\\\n        // device_type属性标识了该节点代表的硬件类型，这里是内存\\\n        device_type = \\\"memory\\\";\\\n        // reg属性定义了内存的起始地址和大小\\\n        // 第一个值是内存的起始地址，第二个值是内存的大小\\\n        // 下面的值被注释掉，用于展示不同的配置选项，当前启用的是128MB的RAM配置\\\n        // reg = <0x0 0x10000000>; //256MB RAM\\\n        reg = <0x0 0x8000000>; //128MB RAM\\\n        // reg = <0x0 0x4000000>; //64MB RAM\\\n    };\\\n\\\n    //pinctrl的节点，它负责配置处理器的引脚控制（pin control）设置。\\\n    //在嵌入式系统中，pinctrl是管理处理器引脚功能的一种机制。\\\n    pinctrl {\\\n        // state_default：标签，用于定义一组默认的引脚配置\\\n        state_default: pinctrl0 {\\\n            // 以下节点定义了不同的LED相关的引脚组及其功能\\\n            p0_led { //引脚名\\\n                ralink,group = \\\"p0_led_an\\\"; //引脚组\\\n                ralink,function = \\\"ephy\\\"; //引脚功能\\\n            };\\\n            p1_led { \\\n                ralink,group = \\\"p1_led_an\\\";\\\n                ralink,function = \\\"ephy\\\";\\\n            };\\\n            p2_led {\\\n                ralink,group = \\\"p2_led_an\\\";\\\n                ralink,function = \\\"ephy\\\";\\\n            };\\\n            p3_led {\\\n                ralink,group = \\\"p3_led_an\\\";\\\n                ralink,function = \\\"gpio\\\";\\\n            };\\\n            p4_led {\\\n                ralink,group = \\\"p4_led_an\\\";\\\n                ralink,function = \\\"gpio\\\";\\\n            };\\\n            // 其余的LED配置与上述类似，指定不同的引脚组及其功能\\\n            spis {\\\n                ralink,group = \\\"spis\\\";\\\n                ralink,function = \\\"pwm_uart2\\\";\\\n            };\\\n            pwm1 {\\\n                    ralink,group = \\\"pwm1\\\";\\\n                    ralink,function = \\\"gpio\\\";\\\n            };\\\n            pwm0 {\\\n                    ralink,group = \\\"pwm0\\\";\\\n                    ralink,function = \\\"gpio\\\";\\\n            };\\\n            refclk {\\\n                ralink,group = \\\"refclk\\\";\\\n                ralink,function = \\\"gpio\\\";\\\n            };\\\n            gpio_0 {\\\n                ralink,group = \\\"gpio\\\";\\\n                ralink,function = \\\"gpio\\\";\\\n            };\\\n            i2s {\\\n                    ralink,group = \\\"i2s\\\";\\\n                    ralink,function = \\\"i2s\\\";\\\n            };\\\n        };\\\n        // 定义uart2引脚的特定设置\\\n        uart2_pins: uart2 {\\\n            uart2 {\\\n                ralink,group = \\\"uart2\\\";\\\n                    ralink,function = \\\"pwm\\\";\\\n                };\\\n        };\\\n\\\n    };\\\n    // Palmbus是Mediatek SoC的一个主总线，此处指定了总线的基地址\\\n    palmbus@10000000 {\\\n        i2c@900 {\\\n                status = \\\"okay\\\"; // 启用位于地址0x900的I2C控制器\\\n        };\\\n\\\n        spi@b00 {\\\n            status = \\\"okay\\\"; // 启用位于地址0xb00的SPI控制器\\\n\\\n            m25p80@0 {\\\n                #address-cells = <1>; // SPI设备树节点的寻址信息，定义了地址单元的数量\\\n                #size-cells = <1>; // SPI设备树节点的大小信息，定义了大小单元的数量\\\n                compatible = \\\"en25q64\\\"; // 指定SPI闪存芯片的型号为en25q64\\\n                reg = <0 0>; // 指定设备在SPI总线上的芯片选择（chip select）编号\\\n                linux,modalias = \\\"w25q64\\\", \\\"w25q128\\\", \\\"w25q256\\\"; // 指定该设备的其他可能兼容型号\\\n                spi-max-frequency = <10000000>; // SPI通信的最大频率，单位为Hz\\\n                m25p,chunked-io = <32>; // 定义了分块I/O操作的大小，这里是32字节\\\n\\\n                // 定义了不同的SPI闪存分区\\\n                partition@0 {\\\n                    label = \\\"u-boot\\\"; // 分区名\\\n                    reg = <0x0 0x30000>; // 分区的起始地址和大小（这里是192KB）\\\n                    read-only; // 分区是只读的\\\n                };\\\n\\\n                // u-boot环境变量分区\\\n                partition@30000 {\\\n                    label = \\\"u-boot-env\\\";\\\n                    reg = <0x30000 0x10000>; // 分区的起始地址和大小（这里是64KB）\\\n                    read-only;\\\n                };\\\n\\\n                factory: partition@40000 {\\\n                    label = \\\"factory\\\";\\\n                    reg = <0x40000 0x10000>; // 分区的起始地址和大小（这里是64KB）\\\n                    read-only;\\\n                };\\\n\\\n                // 固件分区\\\n                partition@50000 {\\\n                    label = \\\"firmware\\\";\\\n                    // 不同的注释分区大小对应不同大小的闪存\\\n                    reg = <0x50000 0xfb0000>; // 分区的起始地址和大小（这里是16MB - 320KB）\\\n                };\\\n            };\\\n        };\\\n\\\n        // 启用两个UART（通用异步收发传输器）设备\\\n        uart1@d00 {\\\n                status = \\\"okay\\\"; // 启用位于地址0xd00的UART1\\\n        };\\\n\\\n        uart2@e00 {\\\n                status = \\\"okay\\\"; // 启用位于地址0xe00的UART2\\\n        };\\\n    };\\\n\\\n    // SDHC（Secure Digital Host Controller）配置\\\n    sdhci@10130000 {\\\n        status = \\\"okay\\\"; // 启用SD卡控制器\\\n        mediatek,cd-low; // Mediatek特有的属性，可能表示卡检测（Card Detect）的极性\\\n    };\\\n\\\n    // 以太网控制器配置\\\n    ethernet@10100000 {\\\n        mtd-mac-address = <&factory 0x28>; // 从工厂设置分区中指定偏移读取MAC地址\\\n    };\\\n\\\n    // GPIO控制的LED配置\\\n    gpio-leds {\\\n        compatible = \\\"gpio-leds\\\"; // 指明兼容的设备类型是GPIO控制的LED\\\n\\\n        // 定义系统状态LED\\\n        system {\\\n                label = \\\"mediatek:green:system\\\"; // LED标签\\\n                gpios = <&gpio1 5 1>; // 指定GPIO端口和引脚编号\\\n                default-state = \\\"off\\\"; // 默认状态为关闭\\\n        };\\\n\\\n        // 定义Wi-Fi状态LED\\\n        wifi {\\\n                label = \\\"mediatek:green:wifi\\\"; // LED标签\\\n                gpios = <&gpio1 12 1>; // 指定GPIO端口和引脚编号\\\n                default-state = \\\"off\\\"; // 默认状态为关闭\\\n        };\\\n    };\\\n\\\n    // GPIO按键配置，采用轮询方式\\\n    gpio-keys-polled {\\\n        compatible = \\\"gpio-keys-polled\\\"; // 指明兼容的设备类型是GPIO按键\\\n        #address-cells = <1>;\\\n        #size-cells = <0>;\\\n        poll-interval = <20>; // 定义按键扫描的间隔时间，单位为毫秒\\\n\\\n        // 定义一个GPIO按键\\\n        button_1 {\\\n                label = \\\"button_1\\\"; // 按键标签\\\n                gpios = <&gpio1 6 0>; // 指定GPIO端口和引脚编号\\\n                linux,code = <0x101>; // 按键对应的Linux输入子系统的键值代码\\\n        };\\\n    };  \\\n};\\\n~~~\\\n\\\n### 常见的地址属性：\\\n1. `#adress-cells`地址字段的长度（32位字为单位）\\\n2. `#size-cells`大小字段的长度\\\n3. `reg`内存映射，包括起始地址和范围\\\n4. **`range`**描述PCI地址到物理内存地址的映射\\\n~~~\\\nranges = <\\\n            0x02000000 0 0x00000000 0x20000000 0 0x10000000 /* pci memory */\\\n            0x01000000 0 0x00000000 0x10160000 0 0x00010000 /* io space */\\\n        >;\\\n~~~\\\n1. `0x02000000 0 0x00000000 0x20000000 0 0x10000000`\\\n\\t- 这个条目描述了**PCI内存空间到物理内存的映射**。\\\n\\t- `0x02000000`是PCI地址空间的一个标识，通常对应于该空间类型（例如内存空间）。\\\n\\t- 接下来的 `0` 是PCI总线上的地址（通常为0，表示这是起始地址）。\\\n\\t- `0x00000000` 是转换后的物理地址的起始点。\\\n\\t- `0x20000000` 是物理地址空间的大小。\\\n\\t- 最后的 `0` 和 `0x10000000` 表明这个映射的大小是 0x10000000 字节（256MB）。\\\n2. `0x01000000 0 0x00000000 0x10160000 0 0x00010000`\\\n\\\n\\t- 这个条目描述了**PCI I/O空间到物理内存的映射**。\\\n\\t- `0x01000000` 标记了这是I/O空间的映射。\\\n\\t- 同样地，第二个 `0` 表示PCI I/O空间的开始地址。\\\n\\t- `0x10160000` 是物理地址空间中对应的起始地址。\\\n\\t- 最后的 `0` 和 `0x00010000` 表示这个映射的大小是 0x00010000 字节（64KB）。\\\n\\\n5. `@`后跟数字，通常表示设备在**父总线**或者**父节点**内存映射中的基础地址\\\n\\\n### 引脚配置\\\n通常引脚可配置的属性是在内核源码中就被定义好的，要根据不同的芯片编写\\\n1. 指定引脚所属的组名\\\n2. 指定引脚组的功能\\\n\\\n### 标签\\\n标签用来表示特定的节点或者属性。同时方便重用和覆盖\\\n>lable :\\\n~~~\\\nfactory: partition@40000 {\\\nlabel = \\\"factory\\\";\\\nreg = <0x40000 0x10000>; // 分区的起始地址和大小（这里是64KB）\\\nread-only;\\\n};\\\n~~~\\\n引用语法：`<&lable (偏移地址)>`\"]],\"start1\":0,\"start2\":0,\"length1\":0,\"length2\":7200}]"
metadata_diff: {"new":{"id":"edb9b1f671954234bb9086c0492485e6","parent_id":"edc230fcdbcb45edbe71b8862231999b","latitude":"0.00000000","longitude":"0.00000000","altitude":"0.0000","author":"","source_url":"","is_todo":0,"todo_due":0,"todo_completed":0,"source":"joplin-desktop","source_application":"net.cozic.joplin-desktop","application_data":"","order":1754577307191,"markup_language":1,"is_shared":0,"share_id":"","conflict_original_id":"","master_key_id":"","user_data":"","deleted_time":0},"deleted":[]}
encryption_cipher_text: 
encryption_applied: 0
updated_time: 2025-08-07T14:39:41.367Z
created_time: 2025-08-07T14:39:41.367Z
type_: 13