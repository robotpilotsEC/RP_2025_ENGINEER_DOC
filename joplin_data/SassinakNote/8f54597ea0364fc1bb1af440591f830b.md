id: 8f54597ea0364fc1bb1af440591f830b
parent_id: 282c5732f6624914a9cbd07dfe96a656
item_type: 1
item_id: 185906cbbcca4d178fb0836f16e61516
item_updated_time: 1756523877067
title_diff: "[]"
body_diff: "[{\"diffs\":[[0,\"现同步**\\\n- \"],[1,\"具体来看，任务不断去读信号量，而外设中断不断等待触发事件，发出信号量，使中断和任务达成同步。\\\n\\\n示例：\\\n~~~\\\n#define LONG_TIME 0xffff\\\n#define TICKS_TO_WAIT 10\\\n\\\nSemaphoreHandle_t xSemaphore = NULL;\\\n\\\n/* Repetitive task. */\\\nvoid vATask( void * pvParameters )\\\n{\\\n    /* We are using the semaphore for synchronisation so we create a binary\\\n       semaphore rather than a mutex. We must make sure that the interrupt\\\n       does not attempt to use the semaphore before it is created! */\\\n    xSemaphore = xSemaphoreCreateBinary();\\\n\\\n    for( ;; )\\\n    {\\\n        /* We want this task to run every 10 ticks of a timer. The semaphore\\\n           was created before this task was started.\\\n\\\n           Block waiting for the semaphore to become available. */\\\n        if( xSemaphoreTake( xSemaphore, LONG\\\\_TIME ) == pdTRUE )\\\n        {\\\n            /* It is time to execute. */\\\n\\\n             ...\\\n\\\n            /* We have finished our task. Return to the top of the loop where\\\n               we will block on the semaphore until it is time to execute\\\n               again. Note when using the semaphore for synchronisation with an\\\n               ISR in this manner there is no need to 'give' the semaphore\\\n               back. */\\\n        }\\\n    }\\\n}\\\n\\\n/* Timer ISR */\\\nvoid vTimerISR( void * pvParameters )\\\n{\\\nstatic unsigned char ucLocalTickCount = 0;\\\nBaseType_t xHigherPriorityTaskWoken = pdFALSE;\\\n\\\n    /* A timer tick has occurred. */\\\n\\\n    ... Do other time functions.\\\n\\\n    /* Is it time for vATask() to run? */\\\n    xHigherPriorityTaskWoken = pdFALSE;\\\n    ucLocalTickCount++;\\\n    if( ucLocalTickCount >= TICKS_TO_WAIT )\\\n    {\\\n        /* Unblock the task by releasing the semaphore. */\\\n        xSemaphoreGiveFromISR( xSemaphore, &xHigherPriorityTaskWoken );\\\n\\\n        /* Reset the count so we release the semaphore again in 10 ticks\\\n           time. */\\\n        ucLocalTickCount = 0;\\\n    }\\\n\\\n    /* Yield if xHigherPriorityTaskWoken is true. The \\\n       actual macro used here is port specific. */\\\n    portYIELD_FROM_ISR( xHigherPriorityTaskWoken );\\\n}\\\n\\\n~~~\\\n***\\\n\"]],\"start1\":536,\"start2\":536,\"length1\":8,\"length2\":1987}]"
metadata_diff: {"new":{},"deleted":[]}
encryption_cipher_text: 
encryption_applied: 0
updated_time: 2025-08-30T07:24:53.299Z
created_time: 2025-08-30T07:24:53.299Z
type_: 13