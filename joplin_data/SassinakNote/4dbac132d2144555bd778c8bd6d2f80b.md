id: 4dbac132d2144555bd778c8bd6d2f80b
parent_id: b10fc3fdc05e4ecbaae9098bfe00e679
item_type: 1
item_id: 205c00691630420e86804cbeabbf0e87
item_updated_time: 1756521108945
title_diff: "[]"
body_diff: "[{\"diffs\":[[0,\"# 任务\\\n\"],[1,\"## 任务状态\\\n- 运行（Running）\\\n- 准备（Ready）不处于挂起或已阻止的任务\\\n- 封锁（Blocked）处于阻塞或者等待信号量，事件组的任务\\\n- 暂停（Suspended）由于处于挂起的任务**不会超时**，需要显示命令进入准备或者运行。\\\n![4dbf916c812ef97b604fae8cec26b219.png](:/8805cd8a573e4d0fb359973fe62954e8)\\\n\\\n***\\\n## 任务优先级\\\n- 每个任务都分配了从 0 到 （ configMAX_PRIOR`ITIES - 1 `） 的优先级，其中 `configMAX_PRIORITIES` 在 `FreeRTOSConfig.h `中定义\\\n- 如果未定义 `configUSE_TIME_SLICING`，或者如果 `configUSE_TIME_SLICING` 设置为 1，则具有相同优先级的就绪状态任务将使用时间片轮询调度方案共享可用的处理时间。\\\n***\\\n## 任务调度\\\n- 默认情况下，FreeRTOS 使用`固定优先级抢占式调度策略`，对同等优先级的任务进行`循环时间切片`\\\n- 也可以通过中断改变任务的优先级\\\n\\\n***\\\n## 任务实现\\\n基本结构：\\\n~~~\\\nvoid vATaskFunction( void *pvParameters )\\\n{\\\n    for( ;; )\\\n    {\\\n        -- Task application code here. --\\\n    }\\\n\\\n    /* Tasks must not attempt to return from their implementing\\\n       function or otherwise exit. In newer FreeRTOS port\\\n       attempting to do so will result in an configASSERT() being\\\n       called if it is defined. If it is necessary for a task to\\\n       exit then have the task call vTaskDelete( NULL ) to ensure\\\n       its exit is clean. */\\\n    vTaskDelete( NULL );\\\n}\\\n\\\n~~~\\\n\\\n事件驱动任务：\\\n~~~\\\nvoid vATaskFunction( void *pvParameters )\\\n{\\\n    for( ;; )\\\n    {\\\n        /* Psudeo code showing a task waiting for an event\\\n           with a block time. If the event occurs, process it.\\\n           If the timeout expires before the event occurs, then\\\n           the system may be in an error state, so handle the\\\n           error. Here the pseudo code \\\"WaitForEvent()\\\" could\\\n           replaced with xQueueReceive(), ulTaskNotifyTake(),\\\n           xEventGroupWaitBits(), or any of the other FreeRTOS\\\n           communication and synchronisation primitives. */\\\n        if( WaitForEvent( EventObject, TimeOut ) == pdPASS )\\\n        {\\\n            -- Handle event here. --\\\n        }\\\n        else\\\n        {\\\n            -- Clear errors, or take actions here. --\\\n        }\\\n    }\\\n\\\n    /* As per the first code listing above. */\\\n    vTaskDelete( NULL );\\\n}\\\n~~~\\\n\\\n***\\\n## 任务创建宏\\\n可以选择使用 `portTASK_FUNCTION` 和 `portTASK_FUNCTION_PROTO` 宏定义任务函数。\"]],\"start1\":0,\"start2\":0,\"length1\":5,\"length2\":1971}]"
metadata_diff: {"new":{"order":878260553815.5,"user_updated_time":1756521060047},"deleted":[]}
encryption_cipher_text: 
encryption_applied: 0
updated_time: 2025-08-30T02:32:17.762Z
created_time: 2025-08-30T02:32:17.762Z
type_: 13