id: 5956aef232ce40f3a513363d18a2fe3c
parent_id: 
item_type: 1
item_id: 8e7cb7df03cc4dff91f3ff2c7883e315
item_updated_time: 1755002798246
title_diff: "[{\"diffs\":[[1,\"设备树的加载流程\"]],\"start1\":0,\"start2\":0,\"length1\":0,\"length2\":8}]"
body_diff: "[{\"diffs\":[[1,\"# 设备树的加载流程\\\n## 地址设置\\\n一般通过 `Bootloader` 引导启动 `Kernel`，在启动 `Kernel` 之前，`Bootloader` 必须将 `dtb` 文件的**首地址**传输给 `Kernel`，以供使用。\\\n\\t1. `Bootloader` 将 `dtb` **二进制文件**的**起始地址**写入 `r2` 寄存器中\\\n\\t2. `Kernel` 在第一个启动文件 `head.S/head-common.S` 中，读取 `r2` 寄存器中的值，获取 `dtb` 文件起始地址\\\n\\t3. 跳转入口函数 `start_kernel` 执行C语言代码\\\n***\\\n## 获取平台信息  --machine_desc\\\n在 `dts` 文件中，在根节点中有一个 `compatible` 属性，该属性的值是一系列的**字符串**，比如\\\n~~~\\\n\\tcompatible = “samsung，smdk2440”“samsung,smdk2410,samsung，smdk24xx”;\\\n~~~\\\n\\\n该属性就是告诉内核要选择什么样的 `machine_desc`，因为 `machine_desc` 结构体中有一个 `dt_compat` 成员，该成员表示 `machine_desc` 支持哪些单板，所以内核会把 `compatible` 中的字符串与 `dt_compat` 进行依次比较。\\\n***\\\n## 获取配置信息\\\n  设备树中的 `chosen`属性，用于传输固件和系统之间的数据，也包含一些启动参数\\\n  1. `/chosen `节点中 `bootargs` 属性的值, 存入全局变量： `boot_command_line`\\\n  2. 确定根节点的这2个属性的值: `#address-cells`,` #size-cells`\\\n  3. 存入全局变量: `dt_root_addr_cells`, `dt_root_size_cells`\\\n  4. 解析`/memory`中的 `reg` 属性, 提取出`\\\"base, size\\\"`, 最终调用 `memblock_add(base, size)`;\\\n***\\\n## 解析设备树节点\\\n`dtb`文件将会一直存在，**不会被内核或者进程**占用，可以直接使用 `dtb`文件，生成多个 `device_node`，生成的根节点为：**`of_root`**\\\n\\\n>每一个节点都以TAG(FDT_BEGIN_NODE, 0x00000001)开始, 节点内部可以嵌套其他节点,\\\n每一个属性都以TAG(FDT_PROP, 0x00000003)开始\\\n\\\n**device_node结构体**\\\n~~~\\\n struct device_node {\\\n            const char *name;  // 来自节点中的name属性, 如果没有该属性, 则设为\\\"NULL\\\"\\\n            const char *type;  // 来自节点中的device_type属性, 如果没有该属性, 则设为\\\"NULL\\\"\\\n            phandle phandle;\\\n            const char *full_name;  // 节点的名字, node-name[@unit-address]\\\n            struct fwnode_handle fwnode;\\\n\\\n            struct  property *properties;  // 节点的属性\\\n            struct  property *deadprops;    /* removed properties */\\\n            struct  device_node *parent;   // 节点的父亲\\\n            struct  device_node *child;    // 节点的孩子(子节点)\\\n            struct  device_node *sibling;  // 节点的兄弟(同级节点)\\\n        #if defined(CONFIG_OF_KOBJ)\\\n            struct  kobject kobj;\\\n        #endif\\\n            unsigned long _flags;\\\n            void    *data;\\\n        #if defined(CONFIG_SPARC)\\\n            const char *path_component_name;\\\n            unsigned int unique_id;\\\n            struct of_irq_controller *irq_trans;\\\n        #endif\\\n        };\\\n~~~\\\n\\\n**那么多的 `device_node`，哪些会被转化为 `platform_device` 呢？**\\\n>根节点下的子节点，且该子节点必须包含 `compatible` 属性；\\\n如果一个节点的 `compatile` 属性含有这些特殊的值(`“simple-bus”`,`“simple-mfd”`,`“isa”`,`“arm,amba-bus”`)之一，那么它的子结点(需含 `compatile` 属性)也可以转换为 `platform_device`。\\\n***\\\n\\\n## 内核API获取设备树属性\\\n~~~\\\na. 处理DTB\\\nof_fdt.h // dtb文件的相关操作函数, 我们一般用不到, 因为dtb文件在内核中已经被转换为device_node树(它更易于使用)\\\n\\\nb. 处理device_node\\\nof.h // 提供设备树的一般处理函数, 比如 of_property_read_u32(读取某个属性的u32值), of_get_child_count(获取某个device_node的子节点数)\\\nof_address.h // 地址相关的函数, 比如 of_get_address(获得reg属性中的addr, size值)\\\nof_match_device(从matches数组中取出与当前设备最匹配的一项)\\\nof_dma.h // 设备树中DMA相关属性的函数\\\nof_gpio.h // GPIO相关的函数\\\nof_graph.h // GPU相关驱动中用到的函数, 从设备树中获得GPU信息\\\nof_iommu.h // 很少用到\\\nof_irq.h // 中断相关的函数\\\nof_mdio.h // MDIO (Ethernet PHY) API\\\nof_net.h // OF helpers for network devices.\\\nof_pci.h // PCI相关函数\\\nof_pdt.h // 很少用到\\\nof_reserved_mem.h // reserved_mem的相关函数\\\n\\\nc. 处理 platform_device\\\nof_platform.h // 把device_node转换为platform_device时用到的函数,\\\n// 比如of_device_alloc(根据device_node分配设置platform_device),\\\n// of_find_device_by_node (根据device_node查找到platform_device),\\\n// of_platform_bus_probe (处理device_node及它的子节点)\\\nof_device.h // 设备相关的函数, 比如 of_match_device****\\\n~~~\\\n***\\\n## 总结\\\n**dts->dtb->device_node->platform_device**\\\n![7a370786c26a287471c36898d28bfad0.png](:/e23d5f8ae5a746f8b3ecd91fa442227e)\"]],\"start1\":0,\"start2\":0,\"length1\":0,\"length2\":3336}]"
metadata_diff: {"new":{"id":"8e7cb7df03cc4dff91f3ff2c7883e315","parent_id":"b7295ef66afc45749fa9043ab0d7f2c1","latitude":"22.28420000","longitude":"114.17590000","altitude":"0.0000","author":"","source_url":"","is_todo":0,"todo_due":0,"todo_completed":0,"source":"joplin-desktop","source_application":"net.cozic.joplin-desktop","application_data":"","order":0,"markup_language":1,"is_shared":0,"share_id":"","conflict_original_id":"","master_key_id":"","user_data":"","deleted_time":0},"deleted":[]}
encryption_cipher_text: 
encryption_applied: 0
updated_time: 2025-08-12T12:48:40.732Z
created_time: 2025-08-12T12:48:40.732Z
type_: 13