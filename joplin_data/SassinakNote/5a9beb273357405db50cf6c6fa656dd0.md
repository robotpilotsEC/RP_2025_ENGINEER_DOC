id: 5a9beb273357405db50cf6c6fa656dd0
parent_id: 
item_type: 1
item_id: 2c860c2cb25f4fb5bd4d419d70466051
item_updated_time: 1754576827415
title_diff: "[{\"diffs\":[[1,\"类的构造函数\"]],\"start1\":0,\"start2\":0,\"length1\":0,\"length2\":6}]"
body_diff: "[{\"diffs\":[[1,\"## 类的构造函数\\\n\\\n***\\\n\\\n1. 基本知识：构造函数是一种特殊的成员函数，它在对象创建时**自动调用**，用于初始化对象的成员变量。其**名称必须与类名相同**，且没有返回类型\\\n\\\n2.构造函数默认没有参数，带参数的构造函数可以灵活的初始化对象\\\n`MyClass(int val) : x(val)`\\\n>x是对象的一个成员\\\n\\\n\\\n构造函数的执行顺序是：\\\n1. 执行基类的构造函数\\\n2. 执行基类里面的成员对象的构造函数\\\n3. 执行派生类的构造函数\\\n\\\n初始化列表：\\\n初始化列表是构造函数的一种**语法糖**，用于在对象创建时直接初始化成员变量，而不是在构造函数体内赋值。这样做不仅更加高效，而且在某些情况下是必须的，比如 `const` 成员变量和引用类型**必须在初始化列表中初始化**\\\n\\\n\\\n***\\\n## 拷贝构造函数：\\\n>MyClass(const MyClass &obj) : x(obj.x)\\\n\\\n- 将一个对象传递给函数的值参数。\\\n\\\n```class MyClass {\\\npublic:\\\n    int value;\\\n\\t\\\n    // 构造函数\\\n    MyClass(int v) : value(v) {\\\n        std::cout << \\\"普通构造函数\\\" << std::endl;\\\n    }\\\n\\\n    // 拷贝构造函数\\\n    MyClass(const MyClass& other) : value(other.value) {\\\n        std::cout << \\\"拷贝构造函数\\\" << std::endl;\\\n    }\\\n};\\\n\\\n// 以值传递参数，触发拷贝构造\\\nvoid printObject(MyClass obj) {\\\n    std::cout << \\\"对象的值: \\\" << obj.value << std::endl;\\\n}\\\n\\\nint main() {\\\n    MyClass a(42);\\\n    printObject(a); // 这里会调用拷贝构造函数\\\n    return 0;\\\n}\\\n```\\\n\\\n\\\n- 用一个对象初始化另一个对象。\\\n\\\n```#include <iostream>\\\n\\\nclass MyClass {\\\npublic:\\\n    int value;\\\n    MyClass(int v) : value(v) {}\\\n    // 拷贝构造函数\\\n    MyClass(const MyClass& other) : value(other.value) {\\\n        std::cout << \\\"拷贝构造函数被调用\\\" << std::endl;\\\n    }\\\n};\\\n\\\nint main() {\\\n    MyClass a(10);         // 普通构造\\\n    MyClass b = a;         // 用a初始化b，调用拷贝构造\\\n    std::cout << \\\"b.value = \\\" << b.value << std::endl;\\\n    return 0;\\\n}\\\n```\\\n\\\n***\\\n\\\n## 移动构造函数\\\n\\\n```\\\nMyClass(MyClass&& other) noexcept {\\\n        data = other.data;      // 直接“窃取”资源\\\n        other.data = nullptr;   // 原对象指针置空\\\n        std::cout << \\\"移动构造函数\\\" << std::endl;\\\n    }\\\n    ~MyClass() {\\\n        delete data;\\\n    }\\\n```\\\n\\\n必须注意的是，这里涉及到了***动态内存管理***，必须写好析构函数\"]],\"start1\":0,\"start2\":0,\"length1\":0,\"length2\":1565}]"
metadata_diff: {"new":{"id":"2c860c2cb25f4fb5bd4d419d70466051","parent_id":"dd57862bb29c4ba88e2b03b9af44d814","latitude":"0.00000000","longitude":"0.00000000","altitude":"0.0000","author":"","source_url":"","is_todo":0,"todo_due":0,"todo_completed":0,"source":"joplin-desktop","source_application":"net.cozic.joplin-desktop","application_data":"","order":1754577292026,"markup_language":1,"is_shared":0,"share_id":"","conflict_original_id":"","master_key_id":"","user_data":"","deleted_time":0},"deleted":[]}
encryption_cipher_text: 
encryption_applied: 0
updated_time: 2025-08-07T14:39:41.220Z
created_time: 2025-08-07T14:39:41.220Z
type_: 13