id: 3b6c9b43595a43609af40a0a9590587e
parent_id: 
item_type: 1
item_id: 00dcc8bbd11b4dceaa79ea70462846a4
item_updated_time: 1754576827409
title_diff: "[{\"diffs\":[[1,\"多态关键点\"]],\"start1\":0,\"start2\":0,\"length1\":0,\"length2\":5}]"
body_diff: "[{\"diffs\":[[1,\"# 多态的关键点\\\n\\\n1. 虚函数（`Virtual Functions`）：\\\n\\\n- 在基类中声明一个函数为虚函数，使用关键字`virtual`。\\\n- 派生类可以重写（`override`）这个虚函数。\\\n- 调用虚函数时，会根据对象的实际类型来决定调用哪个版本的函数。\\\n \\\n2. 动态绑定（`Dynamic Binding`）：\\\n\\\n- 也称为晚期绑定（`Late Binding`），在运行时确定函数调用的具体实现。\\\n- 需要使用**指向基类的指针**或**引用**来调用虚函数，编译器在运行时根据对象的实际类型来决定调用哪个函数。\\\n  \\\n3. 纯虚函数（`Pure Virtual Functions`）：\\\n\\\n- 一个包含纯虚函数的类被称为抽象类（Abstract Class），它**不能被直接实例化**。\\\n- 纯虚函数没有函数体，声明时使用= 0。\\\n- 它**强制**派生类提供具体的实现。\\\n\\\n\\\n~~~\\\n#include <iostream>\\\nusing namespace std;\\\n\\\n// 抽象类，包含纯虚函数\\\nclass Shape {\\\npublic:\\\n    // 纯虚函数，没有函数体，使用=0声明\\\n    virtual double area() const = 0;\\\n    // 可以包含普通成员函数\\\n    void info() const {\\\n        cout << \\\"This is a shape.\\\" << endl;\\\n    }\\\n};\\\n\\\n// 派生类，必须实现纯虚函数\\\nclass Rectangle : public Shape {\\\npublic:\\\n    Rectangle(double w, double h) : width(w), height(h) {}\\\n    // 实现纯虚函数\\\n    double area() const override {\\\n        return width * height;\\\n    }\\\nprivate:\\\n    double width, height;\\\n};\\\n\\\nint main() {\\\n    // Shape s; // 错误！抽象类不能实例化\\\n\\\n    Rectangle rect(3.0, 4.0);\\\n    rect.info(); // 继承自基类的普通函数\\\n    cout << \\\"Area: \\\" << rect.area() << endl; // 输出: Area: 12\\\n\\\n    // 可以用基类指针指向派生类对象\\\n    Shape* ptr = &rect;\\\n    cout << \\\"Area via base pointer: \\\" << ptr->area() << endl; // 输出: Area via base pointer: 12\\\n\\\n    return 0;\\\n}\\\n~~~\\\n\\\n## 虚函数和纯虚函数的对比\\\n![78d3f128b90c52d29a1c908699927b91.png](:/6d1567dadabc4d5b83900494317b4a19)\\\n\\\n***\\\n## 多态的实现机制：\\\n\\\n**虚函数表**（V-Table）：\\\n- C++运行时使用虚函数表来实现多态。每个包含虚函数的类都有一个虚函数表，表中存储了**指向类中所有虚函数**的指针。\\\n\\\n- 虚函数表是一个指针数组，每个元素都是**虚函数的地址**\\\n\\\n- 如果子类**重写**了虚函数，那么虚函数表中对应的指针就会指向子类自己的实现。\\\n\\\n- 虚函数指针（V-Ptr）：对象中包含一个指向该类虚函数表的指针。这个对象是个隐藏的指针成员，在被**构造**时初始化\\\n\\\n- 当你通过基类指针（或引用）调用虚函数时，实际执行的过程是：\\\n1. 通过对象内部的vptr找到对象实际类型的vtable。\\\n2. 在vtable中查找该虚函数的指针。\\\n3. 跳转到该指针指向的函数实现（基类的还是子类的，取决于实际对象类型）。\\\n\\\n~~~\\\n#include <iostream>\\\nusing namespace std;\\\n\\\nclass Base {\\\npublic:\\\n    virtual void show() { cout << \\\"Base::show()\\\" << endl; }\\\n};\\\n\\\nclass Derived : public Base {\\\npublic:\\\n    void show() override { cout << \\\"Derived::show()\\\" << endl; }\\\n};\\\n\\\nint main() {\\\n    Base* p = new Derived();\\\n    p->show(); // 实际调用的是Derived::show()\\\n    delete p;\\\n    return 0;\\\n}\\\n~~~\\\n\\\n***\\\n## 注意事项：\\\n\\\n1. 只有通过**基类**的指针或引用调用虚函数时，才会发生多态。\\\n2. 如果直接使用派生类的对象调用函数，那么调用的是派生类中的版本，而不是基类中的版本。\\\n3. 多态性需要运行时类型信息（**RTTI**），这可能会增加程序的开销。\\\n\\\n\"]],\"start1\":0,\"start2\":0,\"length1\":0,\"length2\":2150}]"
metadata_diff: {"new":{"id":"00dcc8bbd11b4dceaa79ea70462846a4","parent_id":"23b646d57a31410c9b1e271b32aba572","latitude":"0.00000000","longitude":"0.00000000","altitude":"0.0000","author":"","source_url":"","is_todo":0,"todo_due":0,"todo_completed":0,"source":"joplin-desktop","source_application":"net.cozic.joplin-desktop","application_data":"","order":1754577291809,"markup_language":1,"is_shared":0,"share_id":"","conflict_original_id":"","master_key_id":"","user_data":"","deleted_time":0},"deleted":[]}
encryption_cipher_text: 
encryption_applied: 0
updated_time: 2025-08-07T14:39:41.092Z
created_time: 2025-08-07T14:39:41.092Z
type_: 13