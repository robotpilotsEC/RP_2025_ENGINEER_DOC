id: 1e0e48ded8fb4267802cc0c0b87eb3dc
parent_id: 8701cb4c5b764db28bda61ab5392139b
item_type: 1
item_id: edb9b1f671954234bb9086c0492485e6
item_updated_time: 1755002568075
title_diff: "[]"
body_diff: "[{\"diffs\":[[0,\"载流程\\\n\"],[-1,\"- **地址设置**\\\n一般通过 `Bootloader` 引导启动 `Kernel`，在启动 `Kernel` 之前，`Bootloader` 必须将 `dtb` 文件的**首地址**传输给 `Kernel`，以供使用。\\\n\\\n\\t1. `Bootloader` 将 `dtb` **二进制文件**的**起始地址**写入 `r2` 寄存器中\\\n\\t2. `Kernel` 在第一个启动文件 `head.S/head-common.S` 中，读取 `r2` 寄存器中的值，获取 `dtb` 文件起始地址\\\n\\t3. 跳转入口函数 `start_kernel` 执行C语言代码\\\n\\\n- **获取平台信息  --machine_desc**\\\n\\t在 `dts` 文件中，在根节点中有一个 `compatible` 属性，该属性的值是一系列的**字符串**，比如\\\n\\t~~~\\\n\\tcompatible = “samsung，smdk2440”“samsung,smdk2410,samsung，smdk24xx”;\\\n\\t~~~\\\n\\t该属性就是告诉内核要选择什么样的 `machine_desc`，因为 `machine_desc` 结构体中有一个 `dt_compat` 成员，该成员表示 `machine_desc` 支持哪些单板，所以内核会把 `compatible` 中的字符串与 `dt_compat` 进行依次比较。\\\n\\\n- **获取配置信息**\\\n  设备树中的 `chosen`属性，用于传输固件和系统之间的数据，也包含一些启动参数\\\n  1. `/chosen `节点中 `bootargs` 属性的值, 存入全局变量： `boot_command_line`\\\n  2. 确定根节点的这2个属性的值: `#address-cells`,` #size-cells`\\\n  3. 存入全局变量: `dt_root_addr_cells`, `dt_root_size_cells`\\\n  4. 解析`/memory`中的 `reg` 属性, 提取出`\\\"base, size\\\"`, 最终调用 `memblock_add(base, size)`;\\\n\\\n- **解析设备树节点**\\\n`dtb`文件将会一直存在，**不会被内核或者进程**占用，可以直接使用 `dtb`文件，生成多个 `device_node`，生成的根节点为：**`of_root`**\\\n\\\n>每一个节点都以TAG(FDT_BEGIN_NODE, 0x00000001)开始, 节点内部可以嵌套其他节点,\\\n每一个属性都以TAG(FDT_PROP, 0x00000003)开始\\\n\\\n**device_node结构体**\\\n~~~\\\n struct device_node {\\\n            const char *name;  // 来自节点中的name属性, 如果没有该属性, 则设为\\\"NULL\\\"\\\n            const char *type;  // 来自节点中的device_type属性, 如果没有该属性, 则设为\\\"NULL\\\"\\\n            phandle phandle;\\\n            const char *full_name;  // 节点的名字, node-name[@unit-address]\\\n            struct fwnode_handle fwnode;\\\n\\\n            struct  property *properties;  // 节点的属性\\\n            struct  property *deadprops;    /* removed properties */\\\n            struct  device_node *parent;   // 节点的父亲\\\n            struct  device_node *child;    // 节点的孩子(子节点)\\\n            struct  device_node *sibling;  // 节点的兄弟(同级节点)\\\n        #if defined(CONFIG_OF_KOBJ)\\\n            struct  kobject kobj;\\\n        #endif\\\n            unsigned long _flags;\\\n            void    *data;\\\n        #if defined(CONFIG_SPARC)\\\n            const char *path_component_name;\\\n            unsigned int unique_id;\\\n            struct of_irq_controller *irq_trans;\\\n        #endif\\\n        };\\\n~~~\"],[0,\"\\\n\"]],\"start1\":1063,\"start2\":1063,\"length1\":2063,\"length2\":5}]"
metadata_diff: {"new":{},"deleted":[]}
encryption_cipher_text: 
encryption_applied: 0
updated_time: 2025-08-12T12:48:40.708Z
created_time: 2025-08-12T12:48:40.708Z
type_: 13